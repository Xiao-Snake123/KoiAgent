# 统一换行符策略
#
# 目的：仓库内文本文件一律以 LF 存储，检出时由 git 按平台自动转换。
# 这样在 Windows 下编辑器写出的 CRLF 不会再产生「整文件重写」式的 diff 噪音。
#
# 背景：曾经因为 README.md 被写成混合编码（含 NUL 字节），git 判定其为二进制文件
# 而跳过换行符规范化，导致一次 345 删 / 347 增的无效改动。此文件用于防止复发。

* text=auto

# 二进制资源不参与换行转换
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.pdf   binary
*.woff  binary
*.woff2 binary

# 数据文件不做文本转换（SQLite 等）
*.db    binary
*.sqlite binary
